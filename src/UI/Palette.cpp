#include "UI/Palette.h"

namespace GLPaint
{
    namespace UI
    {
        void RenderPalette(Canvas &canvas)
        {
            if (ImGui::Begin("Palette"))
            {
                ImGui::End();
            }
        }
    }
}
